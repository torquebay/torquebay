{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node" },
    { "src": "public/**/*", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/server.js" },
    { "handle": "filesystem" },                 // 👈 This allows normal HTML routing
    { "src": "/(.*)", "dest": "/public/index.html" }
  ]
}
